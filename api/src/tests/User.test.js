import supertest from "supertest";
import server from "../server";

describe("Users endpoint", () => {
  it("Should return all polls", () => {
    expect(1 + 1).toBe(2); // TODO
  });
});

describe("User endpoint", () => {
  it("Should post a user with valid input", () => {
    expect(1 + 1).toBe(2); // TODO
  });
});
